<h4>Team Members</h4>

<ul class="members-list list-unstyled">
</ul>

<% if (board.get('owned') === true){ %>
  <button class="btn btn-primary"><span class="glyphicon glyphicon-plus"></span> New Member</button>
<% } %>

<div class="new-board-membership">
</div>